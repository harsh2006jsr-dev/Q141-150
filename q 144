Q144: Write a function that accepts a structure as parameter and prints its members.

/*
Sample Test Cases:
Input 1:
Student: Neha 104 92
Output 1:
Name: Neha | Roll: 104 | Marks: 92

*/
#include <stdio.h>
#include <string.h>

struct Student {
    char name[50];
    int roll;
    float marks;
};

void printStudentDetails(struct Student s) {
    printf("Name: %s | Roll: %d | Marks: %.0f\n", 
           s.name, 
           s.roll, 
           s.marks);
}

int main() {
    struct Student myStudent;

    printf("Enter student name: ");
    if (scanf("%49s", myStudent.name) != 1) return 1; 

    printf("Enter roll number: ");
    if (scanf("%d", &myStudent.roll) != 1) return 1;

    printf("Enter marks: ");
    if (scanf("%f", &myStudent.marks) != 1) return 1;

    printf("\nOutput 1:\n");
    printStudentDetails(myStudent);
    
    return 0;
}